import { Mail, MapPin, Phone } from 'lucide-react'
import ContactForm from '../components/ContactForm'
import PageMeta from '../components/PageMeta'
import Reveal from '../components/Reveal'
import { profile } from '../data/portfolio'
export default function Contact(){return <><PageMeta title="Contact" description="Start a conversation about cloud infrastructure, networking, or security operations work."/><div className="shell grid gap-16 py-20 sm:py-32 lg:grid-cols-[.8fr_1.2fr]"><header><span className="eyebrow">Contact / Open channel</span><h1 className="section-title mt-7">Let’s make<br/><span className="text-accent">something reliable.</span></h1><p className="prose-copy mt-8">Have cloud infrastructure to provision, a network to tighten up, or an environment that needs someone watching it properly? Tell me where things stand.</p><div className="mt-12 space-y-4 font-mono text-xs"><a href={`mailto:${profile.email}`} data-cursor="email" className="flex items-center gap-3 hover:text-accent"><Mail size={16}/> {profile.email}</a><a href={`tel:${profile.phone.replace(/\s/g,'')}`} data-cursor="call" className="flex items-center gap-3 hover:text-accent"><Phone size={16}/> {profile.phone}</a><p className="flex items-center gap-3 text-muted"><MapPin size={16}/> {profile.location} · Remote worldwide</p></div></header><Reveal className="rule-card p-7 sm:p-10"><ContactForm/></Reveal></div></>}
