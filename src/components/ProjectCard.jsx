import { ArrowUpRight } from 'lucide-react'
import { Link } from 'react-router-dom'
import ClipReveal from './ClipReveal'
export default function ProjectCard({project,index=0}) { return <ClipReveal delay={Math.min(index*.08,.24)}><article data-cursor="show me" className="group rule-card relative flex min-h-[360px] flex-col p-6 sm:p-8"><div className="flex justify-between"><span className="eyebrow">Case study / {String(index+1).padStart(2,'0')}</span><ArrowUpRight className="transition group-hover:-translate-y-1 group-hover:translate-x-1"/></div><div className="mt-auto pt-20"><h2 className="text-3xl font-semibold sm:text-4xl"><Link className="after:absolute after:inset-0" to={`/projects/${project.slug}`}>{project.title}</Link></h2><p className="mt-4 max-w-lg leading-7 text-muted">{project.short_description}</p><div className="mt-7 flex flex-wrap gap-2">{project.tech_stack.slice(0,5).map((tech)=><span key={tech.id||tech.name} className="border border-line px-2.5 py-1 font-mono text-[10px] uppercase tracking-wider">{tech.name}</span>)}</div></div></article></ClipReveal> }
