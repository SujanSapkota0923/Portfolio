import { ArrowUpRight } from 'lucide-react'
import Logo from './Logo'
import { profile } from '../data/portfolio'
export default function Footer(){ return <footer className="mt-28 border-t border-line"><div className="shell grid gap-10 py-12 md:grid-cols-2"><div><Logo/><p className="mt-4 max-w-sm text-sm leading-6 text-muted">Hybrid cloud infrastructure, networks that stay up, and operations you can audit.</p></div><div className="flex flex-wrap items-end gap-6 md:justify-end"><a className="eyebrow flex items-center gap-1 hover:text-accent" data-cursor="code" href={profile.github_url}>GitHub <ArrowUpRight size={13}/></a><a className="eyebrow flex items-center gap-1 hover:text-accent" data-cursor="profile" href={profile.linkedin_url}>LinkedIn <ArrowUpRight size={13}/></a></div></div><div className="border-t border-line"><div className="shell flex flex-wrap justify-between gap-3 py-5 font-mono text-[10px] uppercase tracking-widest text-muted"><span>© {new Date().getFullYear()} {profile.name}</span><span>Designed &amp; built with intent</span></div></div></footer> }
